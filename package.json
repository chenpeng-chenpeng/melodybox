{
  "name": "melodybox",
  "version": "1.0.0",
  "description": "无损音乐播放器 - 支持FLAC解码、联网歌词、歌曲下载、插件扩展",
  "main": "index.html",
  "scripts": {
    "build:web": "echo 'Web app ready at www/'",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "build:apk": "npx cap sync android && cd android && ./gradlew assembleDebug && echo 'APK: android/app/build/outputs/apk/debug/app-debug.apk'",
    "init:android": "npx cap add android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "author": "MelodyBox",
  "license": "MIT"
}
